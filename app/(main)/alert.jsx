import { View } from "react-native";
import { Text } from "react-native-paper";
import { styles } from "../../lib/style";

export default function AlertPage() {
    return (
        <View style={styles.colContainer}>
            <Text>This is the alert page.</Text>
        </View>
    );
}