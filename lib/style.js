import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    colContainer: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
        gap: 4,
    },
    rowView: {
        flexDirection: "row",
        gap: 4,
    },
    textStandard: {
        fontSize: 16,
    },
    textLink: {
        fontSize: 16,
        color: "#00004D",
    }
});